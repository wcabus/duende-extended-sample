@model GotSharp.IdSrv.Host.Controllers.Account.RegisterConfirmationViewModel
@inject IViewLocalizer _localizer

<div class="register-confirmation-page d-flex flex-column justify-content-center align-items-center">
    <div class="d-flex flex-column col-login-ui">
        <div class="lead">
            <h1>@_localizer["Title"]</h1>
        </div>
        <div class="d-flex flex-column flex-fill">
            <p class="w-100">@_localizer["Confirmation"]</p>
        </div>
    </div>
</div>